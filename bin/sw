#!/usr/bin/env ruby

require "bundler/setup"
#require "movies"

require 'rest-client'
require 'json'
require 'pry'
require 'require_all'
require_all 'lib'
# You can add fixtures and/or initialization code here to make experimenting
# with your gem easier. You can also use a different console, if you like.

# (If you use this, don't forget to add pry to your Gemfile!)
#require "pry"
#Pry.start

#cant require all, pry, or rest-client are files installed?
    CLI.new.greeting

require "irb"
IRB.start(__FILE__)
#get request in console to get the data to turn instances into object to do what it needs to do 
#to create instances of class without set up methods to check if class has instance of data, if so grab it
#check the class first
#